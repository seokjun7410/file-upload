# 파일 업로드 확장자 차단 정책 고려사항

## 핵심 문제 해결 요약

- 이중 확장자 우회를 막기 위해 파일명의 모든 확장자 구간을 차단 정책과 비교
- 확장자와 클라이언트가 보낸 형식 정보 대신 파일의 실제 내용을 확인해 실행 파일 위장 차단
- 원본 파일명은 표시용으로만 보관하고, 실제 저장 이름은 서버가 생성
- 파일 하나는 10MB, 전체 요청은 12MB로 제한
- 화면 검증과 별개로 서버와 DB에서 확장자 규칙·중복·최대 200개를 관리
- 정책 변경 결과를 알 수 없으면 서버에 다시 조회해 화면 상태 복구
- 사용자 안내용 오류 정보와 운영 기록을 분리
- 파일 저장과 DB 기록이 어긋날 수 있는 상황은 상태와 임시 파일로 관리
- 재시도해도 같은 파일을 중복 저장하지 않도록 요청 ID와 처리 결과 보관
- 허용 목록 전환에 필요한 운영 기준이 정해질 때까지 현재 차단 목록 유지

## 상세 고려사항

### 1. 최종 확장자만 보지 않고 모든 확장자 구간을 검사한다

#### 검토한 위험

행정안전부 「소프트웨어 개발보안 가이드」는 위험한 파일 업로드를 막기 위해
허용 확장자 기반의 allowlist 검증을 권고한다. OWASP도 `.jpg.php` 같은 이중
확장자를 확장자 검증의 대표적인 우회 방법으로 설명하며, 특정 확장자만 차단하는
denylist를 단독 방어책으로 사용해서는 충분하지 않다고 본다. MITRE CWE-434에는
일부 웹 서버가 `filename.php.gif`의 최종 확장자가 아니라 중간 확장자를 기준으로
파일을 처리할 수 있다는 위험도 제시되어 있다.

현재 요구사항은 관리자가 등록한 확장자를 차단하는 방식이다. 최종 확장자만
검사하면 `report.exe.pdf`가 `pdf`로 판정되어 `exe` 차단 정책을 우회한다.
반대로 `exe`라는 문자열이 들어 있기만 해도 차단하면 `report.exefoo.pdf` 같은
정상 파일까지 거부한다.

#### 판단과 근거

- 파일명의 첫 번째 점 뒤에 있는 모든 구간을 확장자 후보로 추출한다.
- 각 구간은 공백을 제거하고 소문자로 바꾼 뒤 차단 정책과 정확히 비교한다.
- 차단 구간이 하나라도 있으면 저장 전에 업로드를 거부한다.
- 여러 차단 구간이 있으면 왼쪽에 있는 첫 구간을 오류 안내에 사용한다.
- `exefoo`는 `exe`와 일치하지 않으며, 차단 정책과 일치하지 않는 정상 복합
  파일명은 허용한다.

```text
차단 정책: exe

report.exe.pdf    → exe 구간과 정확히 일치 → 차단
report.exefoo.pdf → exe 구간과 일치하지 않음 → 허용
```

### 2. 확장자와 클라이언트가 보낸 형식 대신 파일의 실제 내용을 확인한다

#### 검토한 위험

확장자는 사용자가 바꿀 수 있고, 업로드 요청의 `Content-Type`(클라이언트가 보낸
형식 정보)도 신뢰할 수 없다. 실행 파일을 `image.jpg`로 바꾸거나
`Content-Type: image/jpeg`로 전송하면 이 두 정보만으로는 실제 콘텐츠를 판별할
수 없다. 그러나 식별하기 어려운 모든 파일을 차단하면 정상 텍스트와 새로운
형식까지 과도하게 거부할 수 있다.

#### 판단과 근거

- Java 기본 기능인 `Files.probeContentType`도 검토했지만, 운영체제와 서버 설정에
  따라 결과가 달라질 수 있다. Java로 실행되는 현재 애플리케이션에서 서버 환경과
  무관하게 파일 내용을 확인하려고 Apache Tika를 선택했다. `tika-core`만 사용해
  파일 형식 감지 기능만 적용한다.
- Apache Tika는 원본 파일명이나 multipart `Content-Type`을 믿지 않고 파일 바이트를
  바탕으로 파일 형식을 감지한다.
- 감지한 형식이 애플리케이션이 관리하는 실행 파일 형식 목록과 일치하면
  `422 BLOCKED_EXECUTABLE_MIME`으로 거부한다.
- 감지는 완료했지만 형식을 특정하지 못한 `UNKNOWN`은 경고 후 기존 확장자
  정책을 계속 적용한다.
- 파일 내용을 읽는 중 분석에 실패한 `FAILED`는 확인하지 못한 파일을 저장하지 않고
  `500 FILE_TYPE_DETECTION_FAILED`로 거부한다.
- 확장자와 MIME이 다르다는 사실만으로는 거부하지 않는다.

### 3. 원본 파일명과 실제 저장 이름을 분리한다

#### 검토한 위험

원본 파일명을 그대로 저장 경로에 사용하면 저장될 경로와 파일명이 결합되어
의도하지 않은 위치에 저장되거나, 같은 이름의 파일이 서로 덮어써질 수 있다.

#### 판단과 근거

- 실제 저장 이름은 서버가 만든 UUID와 마지막 확장자로 구성한다.
- 원본 파일명은 사용자에게 보여줄 정보로만 보관하며, 저장 경로를 정하는 데는
  사용하지 않는다.
- 저장 위치는 사용자가 아닌 서버 설정으로 정한다.

### 4. 파일 크기는 처리하기 전에 제한한다

#### 검토한 위험

파일을 모두 읽거나 임시로 저장한 뒤 크기를 확인하면, 너무 큰 요청도 처리 자원을
먼저 사용한다. 제한이 없으면 한 번의 업로드가 서버의 메모리와 저장 공간을 오래
차지할 수 있다.

#### 판단과 근거

- 파일 하나는 10MB, 요청 전체는 12MB까지 허용한다.
- 파일 내용이나 정책을 확인하기 전에 이 제한을 적용한다.
- 제한을 넘으면 파일을 저장하지 않고 용량 초과 오류를 반환한다.

### 5. 화면 검증과 별개로 서버와 DB에서 정책 규칙을 지킨다

#### 검토한 위험

화면에서 입력을 제한해도 요청을 직접 보내면 우회할 수 있다. `EXE`, ` exe `,
`exe`처럼 같은 확장자를 여러 방식으로 저장하면 중복 정책이 생기고, 동시에
등록하면 최대 200개 제한을 넘길 수도 있다.

#### 판단과 근거

- 확장자는 공백을 제거하고 소문자로 바꾼 뒤 비교한다.
- 빈 값, 20자를 넘는 값, 점을 포함한 값은 등록하지 못하게 한다.
- 고정 확장자와 직접 등록한 확장자는 하나의 정책 목록으로 관리한다.
- DB에서도 중복과 필수값을 확인한다.
- 직접 등록한 확장자는 동시에 요청해도 최대 200개를 넘지 않게 관리한다.

### 6. 정책 변경 결과를 알 수 없으면 서버 상태로 화면을 다시 맞춘다

#### 검토한 위험

정책 변경 요청 뒤 응답이 사라지면, 서버가 변경하지 못한 것인지 변경은 됐지만
응답만 사라진 것인지 알 수 없다. 화면의 체크 상태를 그대로 두면 실제 정책과
다를 수 있다.

#### 판단과 근거

- 정책의 실제 상태는 서버에 저장된 값을 기준으로 한다.
- 변경 결과를 확인할 수 없으면 전체 정책을 다시 조회해 화면을 맞춘다.
- 다시 조회하지 못하면 성공이나 실패로 단정하지 않고, 확인이 끝날 때까지 추가
  변경을 막는다.

### 7. 사용자 안내와 운영 기록을 나눈다

#### 검토한 위험

오류 문구를 화면의 판단 기준으로 쓰면 문구가 바뀔 때마다 화면도 함께 바꿔야
한다. 또 오류를 추적할 정보가 없으면 사용자는 왜 막혔는지, 운영자는 어떤 요청과
정책 변경이 있었는지 확인하기 어렵다.

#### 판단과 근거

- 화면은 오류 종류(`code`)와 필요한 최소 정보로 안내 문구를 만든다.
- 요청 ID(`requestId`)로 사용자 문의와 서버 기록을 연결한다.
- 원본 파일명과 서버 내부 경로, 상세 오류 내용은 화면에 보내지 않는다.
- 정책을 만들거나 바꾸고 삭제한 기록은 나중에 확인할 수 있도록 남긴다.

### 8. 파일 저장과 DB 기록이 어긋날 수 있는 상황을 관리한다

#### 검토한 위험

파일을 저장하는 일과 DB에 기록하는 일은 동시에 끝나지 않는다. 중간에 서버가
멈추면 파일만 남거나, 처리 중이라는 기록만 남을 수 있다.

#### 판단과 근거

```text
입력·정책 검증
→ 처리 중 상태 기록
→ 임시 위치에 파일 저장
→ 최종 위치로 이동
→ 완료 상태 기록
```

- 저장에 실패하면 임시 파일을 지우고 실패 상태를 남긴다.
- 처리 중 상태가 오래 남으면 실제 파일이 있는지 확인해 완료 또는 실패로 정리한다.
- 완료 상태인 파일만 정상 파일로 취급한다.

### 9. 다시 요청해도 같은 파일을 중복 저장하지 않는다

#### 검토한 위험

파일 저장은 끝났지만 응답이 사라지면, 사용자는 성공 여부를 알 수 없다. 이때 새
요청으로 다시 업로드하면 같은 파일이 두 번 저장될 수 있다.

#### 판단과 근거

- 업로드를 시작할 때 요청 ID(`requestId`)를 만들고, 다시 요청할 때도 같은 값을 보낸다.
- 서버는 이 값과 업로드 상태·결과를 보관한다.
- 이미 완료된 요청이면 새 파일을 저장하지 않고 기존 결과를 반환한다.
- 아직 처리 중이면 다시 저장하지 않고 잠시 뒤 다시 요청하도록 안내한다.

### 10. 허용 목록 전환은 준비된 뒤에 한다

#### 검토한 위험

허용 목록은 등록되지 않은 형식을 기본으로 막는다. 어떤 파일을 허용할지, 누가
승인할지, 잘못 막힌 파일을 어떻게 확인하고 되돌릴지 정하지 않은 채 전환하면
정상 업무 파일도 업로드하지 못할 수 있다.

#### 판단과 근거

- 현재는 차단 목록 방식을 유지한다.
- 허용할 파일 형식과 승인 절차가 정해지기 전에는 허용 목록을 구현하지 않는다.
- 기존 정책의 허용 상태를 그대로 허용 목록으로 바꾸지 않는다.
- 실제 적용 전에 기존 정책과 새 정책의 차이를 확인하고, 일부 범위부터 적용한 뒤
  문제가 있으면 다시 차단 목록 방식으로 돌아갈 수 있어야 한다.
- 새 정책은 이미 저장된 파일에는 적용하지 않는다.
