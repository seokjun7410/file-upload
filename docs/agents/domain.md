# Domain Docs

이 저장소는 단일 컨텍스트로 취급한다.

- 루트의 `CONTEXT.md`가 존재하면 먼저 읽는다.
- 파일 업로드·확장자 정책 작업 전에는 관련 `docs/adr/`를 읽는다.
- ADR과 충돌하는 변경은 구현 전에 문서에서 명시하고 사용자 결정을 받는다.
- 용어가 아직 `CONTEXT.md`에 없으면 기존 ADR과 코드의 용어를 우선 사용한다.
